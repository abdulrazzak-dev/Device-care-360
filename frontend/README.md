# DeviceCare 360 Frontend

Independent React client application communicating exclusively with the API Gateway (`http://localhost:8080`).

## Environment Configuration
Set API Gateway Base URL:
```env
REACT_APP_API_GATEWAY_URL=http://localhost:8080
```
